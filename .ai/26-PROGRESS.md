# 26 — Estado de Avance

## Objetivo

Registrar, de forma viva y siempre actualizada, en qué fase del [24-ROADMAP.md](./24-ROADMAP.md) está realmente el proyecto — a diferencia de ese archivo, que describe qué debe hacer cada fase pero no si ya se hizo. Existe porque este proyecto se construye en sesiones de agente distintas sin memoria compartida entre sí (ver [AGENTS.md](./AGENTS.md)): sin este archivo, la única forma de saber "¿por dónde vamos?" sería releer todo el historial de commits.

## Regla de mantenimiento

**Todo agente que complete o avance una fase debe actualizar este archivo en el mismo commit** (o inmediatamente después): mover la fase a su estado correcto, anotar el/los commit(s), y registrar cualquier decisión de implementación no obvia o deuda técnica generada. No se cierra una fase como "Completa" sin que haya pasado su checklist de Definición de Hecho (ver [AGENTS.md](./AGENTS.md)).

## Estado por fase

| Fase | Nombre | Estado | Commit(s) | Notas |
|---|---|---|---|---|
| 0 | Documentación `.ai/` | ✅ Completa | `9b30909` | 26 archivos + AGENTS.md; ampliado luego con `25-MVP-SCOPE.md` y este archivo |
| 1 | Foundation | ✅ Completa | `3cb5250`, `33eca47`, `2ce53c7`, `fd01bee` | Scaffold Laravel 13 + Inertia/Vue3/TS, Pest, Boost |
| 2 | Auth / Usuarios | ✅ Completa | `f76851d` | Fortify (sesión, sin 2FA/passkeys — ver ADR-031), RBAC vía `Gate::before` + 29 permisos / 8 roles seed |
| 3 | Companies / Employees | ✅ Completa | `fe75590` | Aislamiento de tenant probado (`BelongsToCompany` + `CurrentCompany`); **deuda de `Position`/`Branch` sin CRUD/UI resuelta** en `8bc28e2` (ver más abajo) |
| — | Traducción UI a español (ADR-043) | ✅ Completa | `9e247f0`, `4cffae4` | No es una fase del roadmap; se hizo entre Fase 3 y 4 a pedido explícito del usuario |
| 4 | Contracts | ✅ Completa | `61f8b1b` | `employment_contracts` + `salary_history` + `payroll_information`; effective-dated lookup con `AmbiguousContractException`; no-overlap reforzado en Postgres (EXCLUDE gist) y a nivel de request (fallback para sqlite en tests) |
| 5 | Schedules/Shifts | ✅ Completa | `37943c5`, `3330ec1`, `f750a66` | 6 entidades (`work_schedule_templates/days`, `employee_schedules`, `shifts`, `shift_assignments`, `shift_breaks`); generación desde plantilla (`ShiftGenerator`) con soporte de turnos nocturnos, dobles y partidos; solapamiento rechazado a nivel de servicio; `audit_logs` + `AuditLogger` adelantados de la Fase 13 para auditar reasignaciones excepcionales. UI de turnos manuales/partidos/dobles y reasignación agregada en `3330ec1`; descansos planificados a nivel de plantilla (con generación automática de `shift_breaks`) agregados en `f750a66`, cerrando el `PENDING DECISION` que tenía [08-SHIFTS.md](./08-SHIFTS.md) |
| 6 | Attendance | ✅ Completa (MVP) | `7e397fb`, `f9f6204` | Alcance MVP tal como define [25-MVP-SCOPE.md](./25-MVP-SCOPE.md): eventos de asistencia solo vía `WEB`/`MANUAL` (panel operado por RR.HH./supervisor, permiso nuevo `attendance.record` — se evaluó biometría de huella pero se confirmó con el usuario mantenerla en su lugar del roadmap, Fase 12/POST-MVP, ADR-036) + mecanismo de ajuste completo (`modify`/`add`/`invalidate`, auto-aprobación por RBAC según ADR-032). Sin capa offline/PWA (POST-MVP) y sin `attendance_records` (eso es Fase 7). Ver "Decisiones de implementación no obvias" abajo |
| 7 | Time Calculation Engine | ✅ Completa | `29660b3`, `69a2d2d`, `d94f605`, `e555977`, `4cdcfc4`, `2741585`, `6fa41b4`, `016570f`, `4e7762b`, `4f9f0de` | `labor_rules`/`labor_rule_versions` (effective-dated lookup + no-overlap Postgres/sqlite) + `attendance_records`/`time_calculation_runs` (esta última inmutable, mismo patrón de guarda que `attendance_events` pero duplicado, no reutilizado) + `AttendanceNetEventsResolver` (aplica ajustes aprobados sobre eventos crudos) + `TimeCalculationEngine` (motor 90%+ cobertura, ejemplo numérico del doc verificado exacto) + recálculo disparado por ajuste aprobado (`AttendanceAdjustmentService`) + permisos `labor_rules.*`/`time_calculation.*` + UI mínima (`labor-rules/Index.vue`, `employees/TimeCalculation.vue`). Sin integración de `novelty_records` (Fase 8 no existe todavía) ni disparo automático de recálculo al publicar una regla nueva (`PENDING DECISION` de `09-TIME-CALCULATION.md`, sigue abierto a propósito). Ver "Decisiones de implementación no obvias" abajo |
| 8 | Overtime/Novedades | ✅ Completa | `20254aa`, `3ee0f55`, `054e49c`, `d224fd8`, `6ffca8b`, `ed8a82a`, `c4d7a01`, `38a131d`, `bedfad1`, `ee09edd`, `eabaa2e`, `ef2792b` | 7 tablas nuevas (`leave_types`/`novelty_types`/`holidays` con el nuevo trait `HasPlatformOrCompanyDefault` para el patrón default-de-plataforma-vs-override-de-empresa; `leave_records`/`absence_records`/`novelty_records`/`overtime_records`). CRUD completo solo para `holidays` (roadmap lo pide explícito); `leave_types`/`novelty_types` quedan con esquema+seeder únicamente, sin UI, per `25-MVP-SCOPE.md` ("catálogo extendido... POST-MVP") — seedeados los 4 esenciales (AUSENCIA/PERMISO/VACACIONES/INCAPACIDAD) más el calendario colombiano fijo (sin festivos móviles/Ley Emiliani, ver más abajo). `LeaveRecordService`/`OvertimeRecordService` siguen el mismo template de `AttendanceAdjustmentService` (transacción+auditoría+recálculo post-commit). `TimeCalculationEngine` (Fase 7) ahora escribe `overtime_records` en `DETECTED` de forma idempotente y clasifica ausencia completa como justificada cuando hay una `novelty_records` aprobada cubriendo la fecha — los dos criterios de aceptación obligatorios del roadmap quedaron cubiertos como tests propios. Ver "Decisiones de implementación no obvias" abajo |
| 9 | Payroll | ✅ Completa (alcance recortado) | `197bb93`, `a13a6b8`, `81f2581`, `71049cf`, `7118bbe`, `ca695fc`, `4b62fb5`, `9f8cf7c`, `509580e`, `b6de977`, `329e5f1`, `54a38d1`, `12a8ec2`, `cf4ca0e`, `4eb7a27`, `4d3bba5`, `c303a97` | 6 tablas nuevas (`payroll_periods`, `payroll_concept_definitions`, `payroll_entries`, `payroll_entry_lines`, `payroll_deduction_plans`, `payroll_adjustments`). Liquida solo 3 conceptos monetarios (salario base prorrateado por días calendario, horas extra autorizadas, deducciones fijas) — Seguridad Social (Fase 10, no existe todavía) y recargo festivo/dominical (Time Calculation nunca produjo esa magnitud) quedan fuera, documentados en `10-PAYROLL.md`, no en silencio. Dos `PENDING DECISION` del propio doc quedaron **RESUELTOS** por el propietario del producto: prorrateo por días calendario, y empleado sin asistencia/novedad se bloquea explícitamente (mismo criterio que "contrato ambiguo"). Ciclo completo `open→calculated→approved(opcional)→closed→reopened` (`PayrollPeriodService`) + corrección post-cierre vía `payroll_adjustments` (ADR-026, camino "ajuste en periodo siguiente" como default, probado a fondo). Nuevas claves `monthly_hours_divisor`/`overtime_surcharge_pct` en `labor_rule_versions.parameters` (extensión de contenedor, Fase 7 ya había anticipado esto). Los 3 escenarios de test obligatorios del roadmap (quincena completa, contrato partido, cierre+corrección) cubiertos como tests de integración propios. Ver "Decisiones de implementación no obvias" abajo |
| 10 | Social Security | ✅ Completa | `89f77d6`, `dd288ce`, `ba07585`, `ab1a95d`, `b827df7`, `c129d60`, `7e34678`, `6d3d3de`, `1cf4931`, `6f7ef96`, `c76f3ef` | 4 tablas nuevas (`social_security_entities`, `social_security_concept_definitions`, `social_security_affiliations` con EXCLUDE gist Postgres, `social_security_contributions`) + `payroll_entry_lines.social_security_contribution_id`. Tasas de aporte versionadas reutilizando `labor_rules`/`labor_rule_versions` (`rule_type='SOCIAL_SECURITY_'.code`, sin tabla paralela). Restricción no negociable respetada: **ningún nombre de entidad real ni porcentaje realista en ningún archivo** — CRUD de entidades/conceptos 100% vacío por defecto (a diferencia de `ColombianHolidaySeeder`), toda fixture usa `type='CATEGORY_A'` y tasas `0.10`/`0.20`. Integrado al mismo paso "Calcular" de Payroll (Fase 9), sin ciclo de vida propio. Cambio de entidad a mitad de periodo produce múltiples `social_security_contributions` correctamente atribuidas por sub-rango. Ver "Decisiones de implementación no obvias" abajo |
| 11 | Reports/PDF | ✅ Completa (alcance recortado MVP) | `6111bb2`, `0a30937`, `4e27bbb`, `046fb20`, `69390a6`, `0f81199`, `1cc3270`, `1d4321a` | Alcance recortado a **únicamente el comprobante de nómina en PDF** por decisión explícita del propietario del producto (`25-MVP-SCOPE.md` línea 48) — el resto de Reports (asistencia, horas, costos laborales, historial de auditoría) queda POST-MVP, documentado no en silencio. `generated_documents` (INMUTABLE, versionado, relación polimórfica real `morphTo`/`morphMany` — primera de este tipo en el repo) + `PdfGenerator` (ADR-011, interfaz desacoplada de `barryvdh/laravel-dompdf`) + plantilla Blade. Generación disparada automáticamente al cerrar un periodo (`PayrollPeriodService::close()`, post-commit, `catch(\Throwable)` deliberadamente amplio — un fallo de un comprobante nunca deshace el cierre ni bloquea a los demás empleados). Reabrir+corregir+recerrar regenera el comprobante de **todos** los empleados del periodo (no solo el corregido, decisión confirmada con el usuario), produciendo v2 sin sobrescribir v1 — probado con el test de integración obligatorio del roadmap. Descarga gateada en `payroll.read` (staff únicamente); autoservicio de `EMPLOYEE` queda fuera de alcance, ver Checklist de `PENDING DECISION` abajo. Ver "Decisiones de implementación no obvias" abajo |
| 12 | Biometrics | 🔲 Pendiente | — | |
| 13 | Audit/Hardening | ✅ Completa (alcance recortado) | `06b2b58`, `da94735`, `cf9f1ba`, `81b62f1`, `5ab8641` | Alcance recortado al objetivo literal del roadmap ("garantizar que toda acción sensible... quede auditada, reforzar la seguridad general") — no se inventaron features nuevas bajo el paraguas de auditoría. Guarda de inmutabilidad de dos capas agregada a `AuditLog` (gap real contra un criterio de aceptación literal, único caso entre las 4 tablas inmutables del sistema que no la tenía). `AuditLogger` (usado 15 veces desde Fase 5) conectado a los 6 controladores CRUD que todavía no lo llamaban: `Position`, `Branch`, `Holiday`, `EmployeeSchedule`, `SocialSecurityEntity`, `SocialSecurityConceptDefinition` (decisión explícita del usuario: los 6 completos, no solo los que afectan dinero). 4 ítems de la lista obligatoria de `16-AUDIT.md` (modificar salario, modificar contrato, cambiar permisos en runtime, acceso cross-tenant `SUPER_ADMIN`) no tienen ningún flujo de negocio construido todavía — documentados como deuda explícita (Grupo 8 abajo), no resueltos inventando features de otras fases. `GET /audit/logs` no se construyó — ya diferido a POST-MVP por la propia nota de alcance de Fase 11. Revisión de `20-SECURITY.md` confirmó cumplimiento ya existente (cifrado de `payroll_information`, rate limiting de login, CSRF, higiene de secretos) sin código nuevo necesario. Ver "Decisiones de implementación no obvias" abajo |
| 14 | Testing | ✅ Completa | `a857926`, `abe5a54`, `b9c8906`, `d126530`, `456b055`, `556d7f5`, `6714be4`, `fdab844`, `1f84b3c` | No agrega funcionalidad de negocio — consolida la estrategia de pruebas de las 13 fases anteriores. Auditoría inicial: 10 de los 11 casos obligatorios de `21-TESTING.md` ya estaban cubiertos (turno nocturno como clasificación diferenciada sigue bloqueado por el mismo `PENDING DECISION` legislativo de Fase 7, no es gap de esta fase). Tres gaps reales cerrados: (1) driver de cobertura PCOV habilitado en CI + gate de dos niveles (80% global vía `composer test:coverage`, 90% en `TimeCalculation`/`Payroll` vía `phpunit.coverage-critical.xml` dedicado, ya que `--coverage-filter` solo agrega directorios al `<source>`, nunca lo acota) — **números reales de cobertura pendientes de confirmar en el primer corrido de CI tras el push**, no se fabricó ninguna cifra; (2) test faltante de ADR-018 (fallo de escritura de audit log debe abortar la transacción de negocio) — pasó al primer intento contra el código existente, confirmando que el contrato ya estaba bien implementado, solo sin test; (3) instalación de **Laravel Dusk** (decisión explícita del usuario: navegador real, no tests HTTP-only) con los 3 flujos E2E críticos de `21-TESTING.md`, cada uno verificado contra un Chrome real en esta sesión, no solo escrito: fichaje→cálculo de tiempo (`HR_MANAGER`, sustituto no-biométrico ya que Fase 12 no existe), corrección de asistencia con dos usuarios (`SUPERVISOR` crea, `HR_MANAGER` aprueba — necesario porque un solo `HR_MANAGER` auto-aprueba por ADR-032 y no deja botón "Aprobar" que probar), y cierre de nómina + corrección en el periodo siguiente (`SUPER_ADMIN`, único rol con `calculate`+`close`+`adjust` juntos). Ver "Decisiones de implementación no obvias" abajo |
| 15 | Deployment | ✅ Completa (pendiente de checklist operativo del usuario) | `5f568f6`, `3591962`, `ce55dd2`, `398c82e`, `6bc3c78` | Infraestructura real provisionada en Laravel Cloud durante esta sesión (no solo código): app `salonfamiliarsaas` conectada a `arbendev04/SalonFamiliarSAAS`, región `us-east-2`, PHP 8.4, Postgres 17 Serverless. Entornos `production` y `staging` ambos con primer deploy exitoso verificado en vivo (build → migración real → seeders de plataforma → `/up` sano → frontend renderizando), staging con base de datos propia y aislada (clúster físico distinto). **Corrección real encontrada y documentada, no silenciada**: ADR-040 asumía rollback automático post-cutover, que resultó ser exclusivo de Laravel Cloud Enterprise, no del plan Starter contratado — corregido a rollback manual + smoke test que alerta (decisión confirmada con el usuario). El `PENDING DECISION` del mecanismo de corte de tráfico (`22-DEPLOYMENT.md` línea 49) también quedó resuelto contra documentación oficial verificada en vivo. Pipeline de CI/CD (`deploy.yml`) construido: se dispara tras el éxito del workflow `tests`, despliega a staging automático con smoke test, producción gateada detrás de una aprobación manual de GitHub Environment. **Hallazgo crítico durante esta misma fase**: al revisar por primera vez el historial real de GitHub Actions, se encontró que `composer types:check` venía fallando desde Fase 6 sin que nadie lo notara (146 errores de PHPStan/Larastan) — corregido de raíz en `398c82e`/`6bc3c78` (ver "Deuda técnica conocida" arriba para la causa y el patrón de fix). Sin este arreglo, el pipeline de `deploy.yml` nunca habría podido disparar un deploy real, sin importar qué tan bien configurado estuviera. **No cerrado al 100%**: el pipeline todavía no corrió de verdad (requiere que el usuario complete el checklist operativo — deploy hooks, secrets de GitHub, Environment de producción con reviewers), y los dos criterios de aceptación restantes de `22-DEPLOYMENT.md` (ensayo de restauración de backup, prueba de rollback) quedan pendientes de ejecutar en el dashboard real. El `PENDING DECISION` de RPO/RTO exacto sigue abierto (bloqueado en el SLA oficial de Laravel Cloud, no investigado a fondo esta sesión). Ver "Decisiones de implementación no obvias" abajo |

## Deuda técnica conocida

- **CORREGIDO (Fase 15, commits `398c82e`/`6bc3c78`) — el pipeline de CI llevaba fallando en `composer types:check` (PHPStan/Larastan) desde prácticamente Fase 6, sin que nadie lo notara**, porque la verificación de cada commit de este proyecto solo corría `php artisan test`, nunca el chequeo de tipos completo. Se descubrió recién al revisar por primera vez el historial real de GitHub Actions en Fase 15 (la mayoría de los runs de `tests` aparecían en rojo). Causa raíz real, más amplia de lo que decía la nota anterior: **Larastan no infiere tipos de NINGÚN atributo con cast declarado vía el método moderno `protected function casts(): array`** cuando ese método está anotado con un `@return` genérico (`array<string, string>`) — no es solo un problema de fechas, también rompe casts `array`/`encrypted` (`parameters`, `old_value`/`new_value`, `original_value`/`corrected_value`, etc.), cascando en ~110 de 146 errores reales encontrados. **El fix correcto no es envolver cada sitio de llamada en `Carbon::parse(...)`** (lo que se hizo puntualmente en `EmployeeController::show()` en Fase 3, un parche válido pero que no escala) **sino anotar el modelo una sola vez** con `@property` en el docblock de la clase (`@property \Illuminate\Support\Carbon $start_date`, `@property array<string, mixed> $parameters`, etc.) — resuelve todos los sitios de llamada del proyecto entero de una vez, sin tocar ninguna lógica. Otros 3 patrones menores encontrados y resueltos en la misma limpieza: (1) `find()`/`findOrFail()` con un ID de tipo `mixed` infiere un union ambiguo `Model|Collection` — se resuelve angostando el tipo del ID antes de la llamada; (2) los `newEloquentBuilder()` de los modelos inmutables (`AttendanceEvent`, `AuditLog`, `GeneratedDocument`, `PayrollAdjustment`, `TimeCalculationRun`) tenían un problema de covarianza de genéricos — se resolvió reemplazando el override manual por el atributo nativo `#[UseEloquentBuilder(X::class)]` de Laravel, verificado contra los 26 tests de inmutabilidad existentes; (3) `HasPlatformOrCompanyDefault::scopeEffectiveForCompany()` necesitaba un `@template TModel` propio — un solo fix en el trait resolvió los 12 errores que aparecían repetidos en sus 6 modelos consumidores. **Lección para toda fase futura**: la Definición de Hecho de cualquier tarea debe incluir correr `composer types:check` localmente, no solo `php artisan test` — nunca más asumir que el pipeline de CI está verde sin revisarlo de verdad.
- **`employment_contracts` en Postgres tiene una constraint `EXCLUDE USING gist` real que impide contratos solapados**, pero los tests corren en sqlite (`phpunit.xml`), que no la soporta. La validación de solapamiento equivalente vive también en `StoreEmploymentContractRequest::withValidator()` — cualquier fase futura que inserte `employment_contracts` fuera de ese Form Request debe replicar esa validación o pasar por él.
- **El test suite DEBE correr sobre sqlite en cualquier entorno, CI incluido** — es el supuesto detrás del punto anterior y de todo el patrón "constraint en Postgres + validación de respaldo a nivel de servicio". Esto se rompió una vez en CI: `.github/workflows/tests.yml` definía `DB_CONNECTION=pgsql` (y el resto de `DB_*`) como `env` a nivel de job, lo que filtraba esas variables al paso `composer ci:check` y hacía que Pest corriera contra el Postgres real del servicio en vez de sqlite — `EmploymentContractLookupTest::test_it_rejects_an_ambiguous_lookup...` fallaba con un `QueryException` crudo de Postgres (la constraint `EXCLUDE` bloqueaba el INSERT antes de que el código de la app pudiera lanzar `AmbiguousContractException`). Ojo: agregar `force="true"` a los `<env>` de `phpunit.xml` **no alcanza** para ganarle a una variable de entorno ya exportada por el shell/CI (limitación conocida de PHPUnit con `getenv()` vs `$_ENV`) — el fix real es que esas variables de Postgres solo estén scoped al paso que efectivamente las necesita (`Setup Application`, que corre `php artisan migrate --force` contra el Postgres real para validar el DDL), nunca a nivel de job completo.
- **Un snapshot de auditoría (`old_value`/`new_value` de `audit_logs`) nunca debe construirse con `$model->only($model->getFillable())` si el modelo tiene algún atributo con cast de fecha (`'date:Y-m-d'` o similar).** `->only()` llama a `getAttribute()` internamente, que devuelve la instancia `Carbon` ya casteada — al serializarse a JSON en la columna `old_value`/`new_value`, un `Carbon` se convierte en timestamp ISO-8601 completo (`"2026-12-25T00:00:00.000000Z"`), no en el string `Y-m-d` que el cast del modelo declara. Encontrado en Fase 13 al agregar auditoría a `HolidayController` (campo `date`) y a `EmployeeScheduleController` (`effective_from`/`effective_to`); el fix establecido es un snapshot vía `array_intersect_key($model->toArray(), array_flip($model->getFillable()))`, que sí respeta el formato del cast porque `toArray()` pasa por la serialización normal del modelo. Cualquier controlador futuro que audite un modelo con campos de fecha debe usar este patrón desde el principio, no `->only()`.
- **Nunca usar el cast `'date'` (o `'datetime'`) sin formato para una columna solo-fecha si algún código va a hacer una comparación exacta de string contra ella.** El cast `'date'` puro guarda un valor con sufijo de hora (`"2026-02-10 00:00:00"`) en vez de `"2026-02-10"`; en Postgres la comparación funciona igual porque el motor normaliza por tipo de columna, pero en sqlite (motor de los tests) es una comparación de texto crudo que falla. Usar siempre `'date:Y-m-d'` para columnas de fecha pura — esto NO afecta el problema de Larastan de la nota anterior (ambos formatos fallan igual ahí; el fix real es `Carbon::parse(...)`), así que no hay motivo para usar el cast sin formato.
- **`ShiftAssignment::overlapsForEmployee()` no tiene una constraint equivalente a nivel de Postgres** (a diferencia de `employment_contracts`) — el rango de tiempo vive en `shifts`, no en `shift_assignments`, así que un `EXCLUDE` cruzando ambas tablas requeriría un trigger o desnormalizar el rango. Se dejó deliberadamente solo a nivel de servicio (`StoreShiftRequest`, `StoreShiftAssignmentRequest`) por ahora. La misma limitación aplica, por la misma razón estructural, a la deduplicación de `AttendanceEventRecorder` (ver más abajo).
- **`attendance_devices` existe (modelo + migración + factory, Fase 6) pero sin controlador/UI propio, a propósito** — no hay un dispositivo real que gestionar hasta que la Fase 12 (Biometría) conecte hardware de verdad; construir esa UI ahora sería especulativo (YAGNI). Solo sirve hoy como FK opcional de `attendance_events.device_id`.
- **El scope global `BelongsToCompany` excluye silenciosamente las filas `company_id IS NULL` de CUALQUIER acceso al modelo mientras haya una empresa activa — incluidas las relaciones `belongsTo()`, no solo las queries directas.** Confirmado dos veces de forma independiente en la Fase 8: (1) `LeaveRecordService::generateNoveltyAndAbsence()` leía `$record->leaveType->code` vía la relación `belongsTo`, que hereda el scope — todo `LeaveType` de plataforma (los 4 esenciales sembrados, `company_id=null`) resolvía a `null` y tumbaba `create()`/`approve()` con "Attempt to read property on null" en el camino principal de la funcionalidad; (2) el mismo problema en el eager-load `->with('leaveType')` del controlador. Ambos se arreglaron con `withoutGlobalScope('company')`/`effectiveForCompany()` puntuales. El trait `HasPlatformOrCompanyDefault` (nuevo en esta fase, ver abajo) resuelve el caso de query directa, pero **cualquier relación Eloquent hacia un modelo `DIRECTO/GLOBAL` necesita el mismo tratamiento explícito** — no alcanza con que el modelo tenga el trait, hay que evitar el scope en cada punto de acceso, incluidas las relaciones. Registrado como patrón en `.ai/rules/controllers.md` vía Laravel Boost. **Confirmado una tercera vez en Fase 9**: `PayrollEntryLine::concept()`/`PayrollDeductionPlan::concept()` (relaciones `belongsTo` hacia `PayrollConceptDefinition`, también `DIRECTO/GLOBAL`) nulificaban en silencio cualquier concepto de plataforma (`BASE_SALARY`, `OVERTIME`, `LOAN`, `GARNISHMENT`) al hacer eager-load sin `withoutGlobalScope('company')` — corregido en los controladores de Fase 9. **Evitado a propósito en Fase 10** (`social_security_entities`/`social_security_concept_definitions`, también `DIRECTO/GLOBAL`): todos los modelos nuevos llevan docblocks explícitos de advertencia en cada relación `belongsTo` hacia ellos exigiendo `withoutGlobalScope('company')` en el eager-load, y ningún commit de la fase reintrodujo el bug — primera fase en romper la racha de tres repeticiones independientes.
- **`SetCurrentCompany` corre después de `SubstituteBindings` en `bootstrap/app.php`** — en la primerísima request de una sesión (nada en `Session` todavía), el route-model-binding implícito de rutas anidadas por empleado (`employees/{employee}/...`) resuelve **antes** de que el scope global `BelongsToCompany` tenga por qué filtrar, así que una request "fría" a una ruta con un `{employee}` de otra empresa puede bindear igual (confirmado concretamente en Fase 7: una `POST` en frío a `employees/{empleado-ajeno}/attendance/events` llega a crear el evento). Detectado dos veces de forma independiente (Fase 6, CRUD de Branch/Position; Fase 7, `AttendanceRecordController`) sin corregirse ninguna de las dos veces por estar fuera de alcance del commit que lo encontró — los tests existentes no lo detectan porque siempre hacen un request de "calentamiento" antes. Necesita su propia sesión de trabajo: reordenar el middleware y correr la suite completa para confirmar que ningún test dependía (sin saberlo) del orden actual.

## Checklist de `PENDING DECISION` abiertas

Consolidado de todas las ambigüedades funcionales reales que siguen sin resolver en `.ai/`, agrupadas por urgencia. Cada una vive en detalle en su archivo de origen (columna "Fuente") — esta tabla es solo el índice de seguimiento; al resolver una, actualizar primero el archivo de origen (reemplazar `PENDING DECISION` por `RESUELTO` + ADR si corresponde) y recién después marcarla acá.

**Grupo 1 — Ya implementadas "alrededor" en Fase 6/7, sin decisión de negocio real todavía**

- [ ] Precedencia entre dos ajustes `APPROVED` que corrigen el mismo evento (`original_event_id` repetido) — hoy gana el más reciente por `created_at`, nunca se fusionan. Fuente: `07-ATTENDANCE.md` (Flujo 2, punto 4).
- [ ] Descanso real que se abre (`BREAK_START`) pero nunca se cierra — hoy no se resta del tiempo trabajado y no bloquea el cálculo. Fuente: `09-TIME-CALCULATION.md` (Reglas, punto 6).
- [ ] Dos `leave_records` `APPROVED` con rangos solapados para el mismo empleado — sin constraint que lo impida (a diferencia de `employment_contracts`/`ShiftAssignment`). Hoy gana la novedad `approved` más reciente por `created_at`. Fuente: `04-DOMAIN-MODEL.md` (`leave_records`, tras la máquina de estados).
- [ ] Ausencia completa detectada por el motor SIN ninguna `leave_records` que la justifique — ¿debe generarse igual un `absence_records` con `justified=false`? Hoy solo se escribe desde el lado aprobado (`justified=true`); el caso no justificado vive únicamente en `attendance_records.missing_minutes`. Fuente: `04-DOMAIN-MODEL.md` (`leave_records`, tras la máquina de estados).

**Grupo 2 — Resueltas en Fase 9 (Payroll) por el propietario del producto**

- [x] ~~Algoritmo exacto de prorrateo cuando un contrato cambia a mitad de periodo~~ — **RESUELTO**: días calendario (`salario_mensual / días_del_mes` × días del sub-rango). Criterio provisional explícito, no una fórmula legal colombiana validada. Fuente: `10-PAYROLL.md` (Reglas, "Determinación de qué contrato/salario aplica").
- [x] ~~Comportamiento del motor ante un empleado sin ningún `attendance_record` ni `novelty_record`~~ — **RESUELTO**: se bloquea el cálculo de ese empleado específico (`NoAttendanceOrNoveltyDataException`), mismo criterio que "contrato ambiguo". Fuente: `10-PAYROLL.md` (Casos especiales).

**Grupo 2b — Sigue abierta tras implementar la Fase 10 (Social Security) alrededor**

- [ ] Si el prorrateo de aportes de seguridad social sigue el mismo criterio de días calendario recién resuelto para nómina, o tiene su propia regla. **Criterio provisional implementado** en `PayrollCalculationService::socialSecurityContributionLines()` (días calendario, igual que Payroll) para poder calcular algo hoy — sigue sin validación profesional, no se cierra como resuelta. Fuente: `11-SOCIAL-SECURITY.md` (Casos especiales).

**Grupo 2c — Nuevas, surgidas al implementar Fase 9 (alcance recortado, documentado no en silencio)**

- [x] ~~Línea de aporte a seguridad social en `payroll_entries`~~ — **RESUELTO en Fase 10**: `socialSecurityContributionLines()` genera la línea de deducción (`employee_amount`) enlazada a `SocialSecurityContribution` vía `social_security_contribution_id`; `employer_amount` queda solo trazado en `social_security_contributions`, sin línea propia. Fuente: `10-PAYROLL.md` (Casos especiales), `11-SOCIAL-SECURITY.md`.
- [ ] Recargo festivo/dominical como línea monetaria — bloqueado porque `TimeCalculationEngine` nunca produjo esa magnitud de tiempo (mismo `PENDING DECISION` de legislación de `09-TIME-CALCULATION.md` línea 21). Fuente: `10-PAYROLL.md` (Casos especiales).
- [ ] Tratamiento monetario por tipo de novedad (`novelty_types.affects_payroll`) sin consultar — el régimen legal exacto de pago de cada tipo de licencia (ej. incapacidad) no está validado. Fuente: `10-PAYROLL.md` (Casos especiales).
- [ ] Fórmula de recargo de horas extra (`overtime_surcharge_pct` aplicado de forma aditiva sobre la tarifa horaria: `tarifa_hora * (1 + pct)`) es una interpretación de ingeniería del significado convencional de "recargo", no un porcentaje legal colombiano validado — solo la FORMA de la fórmula está fijada, nunca el valor. Fuente: `app/Services/Payroll/PayrollCalculationService.php::authorizedOvertimeLines()`.

**Grupo 2d — Nueva, surgida al implementar Fase 11 (Reports/PDF)**

- [ ] **Autoservicio de `EMPLOYEE` para descargar su propio comprobante de nómina no está implementado.** `.ai/06-AUTHORIZATION.md` (línea 63) marca `payroll.read` para `EMPLOYEE` como `Y` con la anotación "Solo propio comprobante", pero `RoleSeeder::GRANTS['EMPLOYEE']` no otorga `payroll.read` en absoluto (solo `overtime.request`), y **no existe ningún vínculo `User`↔`Employee` en todo el esquema** (ni `employee_id` en `users`, ni `user_id` en `employees`, ni en `UserCompanyMembership`) para poder scopear "propio" aunque el permiso se otorgara. Confirmado con el usuario como fuera de alcance de Fase 11, coherente con la decisión ya tomada en Fase 6 ("sin autoservicio del empleado", ADR-036) — Fase 11 implementó descarga solo para staff con `payroll.read` (`SUPER_ADMIN`, `COMPANY_OWNER`, `ADMIN`, `PAYROLL_MANAGER`, `ACCOUNTANT`). Resolver esto requeriría un cambio de esquema real (vínculo `User`↔`Employee`) más que una decisión de negocio — se documenta como gap de construcción, no como error de spec. Fuente: `.ai/06-AUTHORIZATION.md` (línea 63), `.ai/14-PDF.md` (línea 77/106).

**Grupo 8 — Nueva, surgida al implementar Fase 13 (Audit/Hardening)**

- [ ] **Modificación de salario** (`salary_history`) — no existe ningún flujo de negocio más allá de la creación inicial junto con el contrato (`EmploymentContractController::store()`). No hay endpoint de "aumento salarial" que auditar. Agregar la llamada a `AuditLogger::record()` cuando esta funcionalidad se construya, no olvidarlo en esa fase futura. Fuente: `16-AUDIT.md` línea 43, `04-DOMAIN-MODEL.md`.
- [ ] **Modificación de contrato** (`employment_contracts`) — solo existe `store()` (creación inicial); no hay ruta ni controlador para editar un contrato existente. Agregar la llamada a `AuditLogger::record()` cuando esta funcionalidad se construya. Fuente: `16-AUDIT.md` línea 44, `10-PAYROLL.md`.
- [ ] **Gestión en runtime de roles/permisos** (asignación/revocación de `role`, cambios en `role_permissions`) — hoy solo existe seed estático (`RoleSeeder`/`PermissionSeeder`), sin ningún endpoint/UI de gestión post-seed. Agregar la llamada a `AuditLogger::record()` cuando esta funcionalidad se construya. Fuente: `16-AUDIT.md` línea 45, `06-AUTHORIZATION.md`.
- [ ] **Acceso cross-tenant de `SUPER_ADMIN`** — no existe ningún mecanismo de bypass/impersonación en el código (`BelongsToCompany` no tiene ninguna rama para `SUPER_ADMIN`, `CurrentCompany` no tiene ningún método cross-tenant). Agregar la llamada a `AuditLogger::record()` cuando esta funcionalidad se construya. Fuente: `16-AUDIT.md` línea 47/73, `15-MULTI-TENANCY.md`.
- [ ] **`GET /audit/logs` (consulta de historial de auditoría)** — no construido en Fase 13, ya diferido a POST-MVP por la propia nota de alcance de Fase 11 ("historial de auditoría" listado como reporte POST-MVP). El permiso `audit.read` ya está sembrado y asignado a roles, listo para cuando se construya. Fuente: `16-AUDIT.md` línea 63-64, `25-MVP-SCOPE.md` línea 48.

**Grupo 3 — Deferida a propósito en Fase 7, solo relevante cuando Payroll exista**

- [ ] Si publicar una `labor_rule_version` nueva dispara recálculo automático de fechas afectadas o solo bajo demanda, y si aplica retroactivo a periodos de nómina ya `CLOSED`. Fuente: `09-TIME-CALCULATION.md` (Flujos, punto 3).

**Grupo 4 — Biometría (Fase 12, POST-MVP explícito — no urgente)**

- [ ] Marco legal exacto para datos biométricos en Colombia, más allá del régimen genérico de Habeas Data — marcado como bloqueante antes de construir cualquier integración biométrica real. Fuente: `12-BIOMETRICS.md` (Dependencias legales).
- [ ] Proveedor de hardware biométrico sin elegir (hay una opción recomendada no cerrada: ZKTeco K40/K40 Pro, ADR-042). Fuente: `12-BIOMETRICS.md` (Proveedor(es) de dispositivo).
- [ ] Política de retención de datos biométricos (plazo de conservación, disparador exacto de borrado). Fuente: `12-BIOMETRICS.md` (~línea 130).
- [ ] Mecanismo de inferencia del tipo de evento cuando el dispositivo no lo envía explícito — depende de qué proveedor se elija (punto anterior). Fuente: `07-ATTENDANCE.md` (Reglas, punto 4).

**Grupo 5 — Cumplimiento legal general, no bloquea desarrollo pero sí producción real**

- [ ] Detalle de implementación de Habeas Data colombiano (Ley 1581 de 2012, Decreto 1377 de 2013) sin validar contra asesoría legal profesional. Fuente: `20-SECURITY.md` (Cumplimiento normativo).

**Grupo 6 — Infraestructura, para cuando se llegue a Deployment (Fase 15)**

- [ ] RPO/RTO exactos de backups no confirmados contra el SLA real de Laravel Cloud. Fuente: `22-DEPLOYMENT.md` (~línea 100).

**Grupo 7 — Nota arquitectónica, no es una ambigüedad de negocio (YAGNI explícito, no bloquea nada hoy)**

- [ ] Mecanismo de comunicación interna entre módulos: bus de eventos en memoria vs. llamadas directas encadenadas — el propio doc dice "se resuelve cuando la necesidad de desacoplamiento sea real". Fuente: `03-ARCHITECTURE.md` (~línea 92).

## Decisiones de implementación no obvias (Fase 15 — Deployment)

- **La región `us-east-2` inicialmente mostró "The selected region is not available"** al crear la aplicación, sin explicación documentada oficialmente — se resolvió reintentando la creación completa de la aplicación (no se encontró la causa raíz, posible condición transitoria de la cuenta/trial; si se repite en el futuro, contactar soporte de Laravel Cloud directamente ya que es un problema de su backend sin visibilidad desde este lado).
- **Postgres 17, no 18** — el clúster de base de datos se creó explícitamente en la versión 17 para que coincida exactamente con `postgres:17-alpine` que usa el pipeline de CI (Fase 9+), eliminando una variable no probada nunca contra la suite de tests.
- **PHP 8.4 explícito, no el 8.5 que Laravel Cloud preselecciona por defecto** — mismo criterio: todo el proyecto se desarrolló y probó contra 8.4, usar una versión mayor nunca probada en producción es un riesgo innecesario sin beneficio.
- **`APP_KEY` no se genera automáticamente en Laravel Cloud** (confirmado contra la documentación oficial, contrario a la suposición inicial) — hay que generarlo localmente con `php artisan key:generate --show` y pegarlo a mano en las variables de entorno de cada ambiente. Nunca compartido en esta conversación por ser un secreto real.
- **Base de datos de staging creada con "Create new and scale to zero"**, no "Use same database" — aislamiento completo de datos frente a producción, confirmado por nombre de clúster físico distinto (`main` para staging vs. el de producción), a bajo costo porque se duerme cuando no se usa.
- **`SESSION_DRIVER=database`/`QUEUE_CONNECTION=database` aceptados como default** en ambos entornos ante la ausencia de un recurso de caché (Redis/Valkey) — reversible en cualquier momento agregando el recurso de Cache y cambiando esas dos variables, sin migración de datos compleja.
- **Los 5 seeders de `DatabaseSeeder` (permisos, roles, catálogo de novedades esenciales, festivos colombianos, catálogo de conceptos de nómina) se corrieron una única vez por entorno vía "Commands"**, no como parte de los deploy commands recurrentes — ninguno crea datos de negocio falsos (empresas/usuarios demo), son 100% catálogos de plataforma, seguros de correr en producción real.
- **No se cargaron datos de prueba/demo en ningún entorno** — los que se usaron en fases anteriores de esta sesión nunca se guardaron en un seeder committeado (efímeros, creados al vuelo en bases de datos locales de sesiones pasadas). Cuando se necesiten datos de prueba reales, se crean frescos en `staging`, nunca en `production`.
- **Deploy pipeline vía `workflow_run`, no re-disparando la suite de tests** — `deploy.yml` espera a que el workflow `tests` termine exitosamente en vez de volver a correr lint/tests/cobertura él mismo, evitando duplicar el trabajo de CI ya cubierto en Fase 14.
- **Producción gateada con un GitHub Environment de aprobación manual**, no con un smoke test 100% automático que decida solo — cumple "staging primero, producción después de validación" del propio `22-DEPLOYMENT.md` sin necesitar que un script determine automáticamente si el staging "se ve bien".

## Decisiones de implementación no obvias (Fase 14 — Testing)

- **Baseline de cobertura local aceptado desde CI, no desde esta máquina Windows/Herd** — PCOV no tiene DLL disponible para Herd en Windows sin instalación manual fuera de alcance; el gate de CI es la fuente de verdad de todos modos.
- **`phpunit.coverage-critical.xml` como archivo separado, nunca `--coverage-filter`** — confirmado leyendo el código fuente de PHPUnit (`Merger.php`): ese flag solo *agrega* directorios al `<source><include>` de `phpunit.xml`, nunca lo reemplaza. Es la única forma real de acotar un gate de cobertura a exactamente `TimeCalculation`/`Payroll`.
- **Gates de cobertura en scripts de Composer separados** (`test:coverage`, `test:coverage-critical`), nunca dentro de `test`/`ci:check` — esos deben seguir funcionando en cualquier máquina sin driver instalado. Costo aceptado: la suite corre 3 veces en CI (pasada normal + 2 de cobertura).
- **Dusk corre en un job de CI separado** (`dusk`), no dentro del job `ci` existente — sus requisitos de entorno (DB de archivo, no `:memory:`; `SESSION_DRIVER=file`, no `array`; servidor HTTP real; Chrome) son incompatibles con el job existente.
- **Bug real encontrado y corregido durante la instalación de Dusk**: el fixer `ordered_imports` de Pint reordenó los `use` de `tests/Pest.php` físicamente después de `pest()->extend(DuskTestCase::class)` — como PHP resuelve `::class` según los imports declarados antes en el archivo, esto evaluaba silenciosamente a la cadena literal `"DuskTestCase"` en vez de `"Tests\DuskTestCase"`, rompiendo toda la suite con un error interno de Pest críptico. Corregido reordenando los imports manualmente. Cualquier archivo futuro que combine `pest()->extend()`/config similar con clases importadas debe verificar este orden explícitamente — Pint no lo detecta como error.
- **`SESSION_DRIVER=array`/`CACHE_STORE=array` (usados por la suite normal) rompen Dusk** — el driver `array` solo persiste durante el ciclo de vida de un único request; como Dusk maneja un `php artisan serve` real en un proceso separado, la cookie de sesión de `loginAs()` no sobrevive entre el login y la siguiente visita bajo `array`. `.env.dusk.local` usa `SESSION_DRIVER=file`/`CACHE_STORE=file`.
- **Un `php artisan serve` lanzado manualmente no garantiza observar el swap de `.env` de Dusk** — Dusk solo intercambia el archivo `.env` durante la vida de su propio proceso hijo de PHPUnit; un `serve` lanzado por separado puede seguir viendo el `.env` real (Postgres). Fix: lanzar `serve` con las mismas variables de entorno explícitas que usa el job `dusk` de CI (`DB_CONNECTION=sqlite`, `DB_DATABASE=database/testing_dusk.sqlite`, etc.), que siempre ganan sobre el archivo `.env` sin importar el timing del swap.
- **Un archivo `public/hot` obsoleto (de un `npm run dev` anterior) rompe el renderizado de Dusk** — Inertia/Vite sirve `<script>` apuntando a un dev-server de Vite que ya no corre, y Vue nunca monta, así que ningún selector de formulario existe. Hay que confirmar que `public/hot` no exista (o borrarlo temporalmente) antes de depurar cualquier otro fallo de Dusk — mordió en 2 de los 3 tests E2E de esta fase.
- **`Browser::script()` de Dusk siempre devuelve un array**, incluso para un solo script — nunca encadenar otro método de Dusk directamente sobre su resultado.
- **Flow B necesita dos usuarios** (`SUPERVISOR` crea, `HR_MANAGER` aprueba) — con un solo `HR_MANAGER` el `create()` se auto-aprueba por ADR-032 y no queda ningún botón "Aprobar" visible que probar.
- **Flow C: "Cerrar" es alcanzable directamente desde `calculated`**, sin pasar por "Aprobar" — confirmado en `PayrollPeriodService::close()` (`in_array($period->status, ['calculated', 'approved', 'reopened'])`) y documentado en su propio docblock como opcional por ADR-034. El test igual ejercita la secuencia completa Calcular→Aprobar→Cerrar, pero "Aprobar" no es un paso obligatorio del flujo.
- **Flow C tiene dos campos `name="reason"` en la misma página** (`Show.vue`): el formulario de "Reabrir" en la barra superior y el de "Solicitar ajuste" dentro de la tabla de entries. Un selector `[name="reason"]` sin acotar escribiría en el campo equivocado — hay que scopear con `table [name="..."]`.
- **`adjustInNextPeriod()` exige un `PayrollEntry` ya calculado en el periodo siguiente** antes de que el formulario "Solicitar ajuste" del periodo cerrado tenga sentido — sin ese paso previo, el controlador deja pasar un 500 crudo sin capturar (`NoOpenNextPayrollPeriodException`), no un error de validación limpio.

## Decisiones de implementación no obvias (Fase 13 — Audit/Hardening)

- **Alcance recortado al objetivo literal del roadmap, no a la lista completa de `16-AUDIT.md`** — 4 de las acciones "obligatorias" del doc (modificar salario, modificar contrato, cambiar permisos, acceso cross-tenant `SUPER_ADMIN`) no tienen ningún código que auditar: construirlas ahora habría significado inventar features enteras de otras fases bajo el paraguas de "auditoría". Se documentaron como deuda explícita (Grupo 8 arriba) en vez de resolverse en silencio, mismo criterio que todo recorte de alcance anterior.
- **`GET /audit/logs` no se construyó** — ya diferido a POST-MVP por la propia nota de alcance de Fase 11 ("historial de auditoría" listado ahí como reporte POST-MVP). El permiso `audit.read` sigue sembrado y sin consumir, listo para cuando corresponda.
- **Guarda de inmutabilidad de dos capas para `AuditLog`, duplicada (no genérica)** — mismo patrón exacto que `PayrollAdjustment`/`GeneratedDocument`: `booted()` + un `AuditLogImmutableBuilder` propio. `AuditLog` era la única de las 4 tablas inmutables del sistema sin esta guarda; `AuditLogger::record()` sigue funcionando sin cambios (creación nunca pasa por `update()`/`delete()`).
- **`AuditLogger` inyectado como parámetro de método en los controladores** (no por constructor) — mismo patrón ya establecido por `ShiftAssignmentController` desde Fase 5, replicado en los 6 controladores nuevos.
- **`old_value`/`new_value` = snapshot completo de atributos fillable** (no un diff manual de solo los campos que cambiaron) — decisión de simplicidad para CRUD plano sin máquina de estados, evita omitir un campo por error. Capturado vía `array_intersect_key($model->toArray(), array_flip($model->getFillable()))`, nunca `->only($fillable)` — ver la nota de "Deuda técnica conocida" arriba sobre por qué `->only()` rompe el formato de campos con cast de fecha.
- **`new_value = null` en `destroy()` (soft-delete)** — ningún call site existente de `AuditLogger` usa un marcador booleano tipo `{deleted: true}`; el propio `action` string ya comunica la transición.
- **`EmployeeScheduleController` recibió un único audit call en `store()`** (no la tríada create/update/delete de los otros 5 controladores) — su `store()` ya es una acción de negocio de "cerrar anterior + crear nueva" envuelta en una sola transacción, mismo patrón que `ShiftAssignmentController::update()` para una operación de reemplazo.
- **`.ai/20-SECURITY.md` verificado cumplido en sus puntos accionables sin escribir código nuevo**: `payroll_information.bank_account_enc` ya tenía cast `encrypted` (Fase 4), rate limiting de `/auth/login` ya configurado explícitamente en `FortifyServiceProvider` (Fase 2), CSRF nativo de Laravel activo, `.env` nunca comprometido en el historial. Los dos `PENDING DECISION` del propio doc (RPO/RTO exacto de Laravel Cloud, detalle de implementación de Habeas Data) siguen abiertos — ya trackeados en Grupo 5/6 del checklist, no duplicados acá.

## Decisiones de implementación no obvias (Fase 11 — Reports/PDF)

- **Alcance recortado a únicamente el comprobante de nómina** — decisión explícita del propietario del producto (`25-MVP-SCOPE.md` línea 48, confirmada de nuevo en esta sesión), no una interpretación del equipo de implementación. El resto de Reports (asistencia, horas, costos laborales, historial de auditoría) queda POST-MVP.
- **`barryvdh/laravel-dompdf` elegido sobre `spatie/laravel-pdf`** (confirmado con el usuario) — PHP puro, sin binario Chromium, encaja mejor con el compute Flex de Laravel Cloud (ADR-021), suficiente para un documento tabular como un comprobante.
- **Relación polimórfica real de Laravel** (`morphTo`/`morphMany` con `Relation::enforceMorphMap(['payroll_entry' => PayrollEntry::class])` en `AppServiceProvider::boot()`) sobre las columnas `reference_entity_type`/`reference_entity_id` del doc — primera relación polimórfica de todo el repo. Más limpio que `where()` a mano y extensible cuando POST-MVP agregue más tipos de documento (`attendance_report`, `period_summary`, etc.), sin requerir cambio de esquema.
- **`generated_documents` no lleva `AuditLogger::record()`** — ni `.ai/14-PDF.md` ni sus criterios de aceptación lo exigen, a diferencia de toda transición de estado de `PayrollPeriodService`/`PayrollAdjustmentService`, que sí lo mandan explícitamente. No inventado como requisito.
- **Generación disparada dentro de `PayrollPeriodService::close()`, después de que su transacción existente confirma (nunca anidada)**, mismo patrón exacto de `AttendanceAdjustmentService::triggerRecalculationForApprovedAdjustment()` (Fase 7) — un efecto secundario derivado nunca puede deshacer la operación primaria ya confirmada. Desviación deliberada del precedente: el catch es `\Throwable` (no una lista acotada de excepciones), documentado inline, porque los modos de falla de renderizado/storage son heterogéneos y ninguno puede bloquear el cierre ni a los demás empleados.
- **Cada `close()` regenera el comprobante de TODOS los empleados del periodo, no solo el corregido** (confirmado con el usuario) — foto inmutable de "así se vio la nómina en este cierre", sin necesidad de detectar qué entry cambió realmente. Es exactamente este diseño sin ramas especiales el que produce v1 en el primer cierre y v2 tras reapertura+corrección+recierre — probado por el test de integración obligatorio del roadmap (`tests/Feature/Payroll/PayrollReceiptVersioningTest.php`).
- **Bug real encontrado y corregido durante el commit de wiring**: `DompdfPdfGenerator::render()` usa `loadView($view, $data)`, que expone cada clave de `$data` como variable de nivel superior en la vista Blade (no un array `$data` único) — la plantilla original asumía lo segundo. Nunca se detectó antes porque el test del servicio (`PayrollReceiptServiceTest`) siempre usaba un `FakePdfGenerator`, nunca ejercitó el renderizado real de principio a fin. Corregido en el mismo commit que lo encontró.
- **Autoservicio de `EMPLOYEE` (descargar "solo propio comprobante") explícitamente fuera de alcance** — no existe vínculo `User`↔`Employee` en el esquema; ver Grupo 2d del checklist de `PENDING DECISION` arriba para el detalle completo.

## Decisiones de implementación no obvias (Fase 10 — Social Security)

- **Restricción no negociable más estricta que en cualquier fase anterior**: a diferencia de `tolerance_minutes`/`overtime_surcharge_pct` (Fase 7/9, donde un valor ilustrativo documentado como placeholder era aceptable), en esta fase ni siquiera un nombre de entidad real (EPS, ARL, AFP, Colpensiones, etc.) ni un porcentaje realista (4%, 8.5%, 12%, 16%) podía aparecer en ningún lado — ni código, ni tests, ni seeders. Por eso, a diferencia de `ColombianHolidaySeeder` o `PayrollConceptCatalogSeeder`, esta fase **no siembra ningún catálogo de entidades real**: entidades y conceptos son 100% creados por la empresa vía CRUD. Verificado con un grep explícito del diff completo de la fase antes de cerrar (sin resultados).
- **Tasas de aporte versionadas reutilizando `labor_rules`/`labor_rule_versions`**, un `LaborRule` por concepto (`rule_type = 'SOCIAL_SECURITY_' . $concepto->code`, resuelto vía `firstOrCreate` desde el controlador de gestión de tasas, y solo lectura — nunca `firstOrCreate` — desde el motor de cálculo) en vez de una tabla paralela. Aplicación directa de ADR-020. `parameters` lleva `employee_pct`/`employer_pct` (fracciones 0–1, no porcentajes-como-100) y `base_concept_codes` (códigos de `payroll_concept_definitions` cuya suma forma la base, resuelta genéricamente — nunca "base = bruto").
- **"Máxima una afiliación activa por entidad/tipo" se interpreta por `type`, no por `entity_id` exacto** — un empleado puede tener una afiliación activa de cada `type` simultáneamente, nunca dos del mismo `type`. Requirió denormalizar `entity_type` en `social_security_affiliations`, poblado por el servicio desde la entidad, nunca escrito directo por el usuario (ni siquiera es un campo de request en `StoreSocialSecurityAffiliationRequest`).
- **`social_security_contributions` no lleva guarda de inmutabilidad a nivel de ORM** — mismo criterio que `payroll_entries`/`payroll_entry_lines` (AJUSTE, Fase 9): disciplina de servicio únicamente, no una tercera clase de guarda duplicada.
- **`SocialSecurityAffiliationService::affiliate()`/`reassign()`**: `reassign()` cierra la afiliación vigente (`end_date = effectiveDate - 1 día`) y abre una nueva en una sola transacción con un único registro de auditoría (no dos), seguido del precedente establecido por `LeaveRecordService`/`OvertimeRecordService`. `SocialSecurityAffiliation::activeFor()` distingue "cero afiliaciones" (resultado `null`, normal — el concepto puede no estar configurado) de "solapamiento/hueco" (`NoActiveSocialSecurityAffiliationException`, un problema real de integridad) — es el propio `reassign()` quien decide si un `null` es un error en su contexto.
- **Resolución de sub-rangos de afiliación distingue explícitamente "cero afiliaciones para todo el periodo" (normal, se salta el concepto) de "hay al menos una afiliación pero con hueco/solapamiento" (bloqueante)** — a diferencia de `resolveContractSubRanges()` (Fase 9), donde todo hueco es siempre un error porque un empleado siempre debe tener contrato. Una afiliación que empieza o termina a mitad del periodo sin cobertura completa también cuenta como hueco bloqueante, sin caso especial de "cobertura parcial aceptable".
- **`employer_amount` no genera ninguna `payroll_entry_line`** — solo queda trazado en `social_security_contributions`, ya que ningún criterio de aceptación exige reflejar el costo patronal en la liquidación del empleado. Cada `payroll_entry_line` de aporte usa `amount = employee_amount` y referencia el concepto genérico `SOCIAL_SECURITY` del catálogo de nómina (no el `SocialSecurityConceptDefinition` específico, que es un catálogo distinto).
- **El controlador de afiliaciones (`SocialSecurityAffiliationController::store()`) no puede tipar `StoreSocialSecurityAffiliationRequest` directamente en su firma para el camino de reasignación** — la validación automática de un Form Request corre antes del cuerpo del método, y el chequeo de solapamiento de `withValidator()` rechaza toda reasignación legítima (la afiliación que se va a cerrar sigue abierta en el momento de validar; recién `reassign()` la cierra dentro de su propia transacción). Se resolvió construyendo el `Validator` manualmente dentro del controlador y aplicando el cierre de solapamiento solo en el camino de alta nueva (`affiliate()`), nunca en el de reasignación (que es libre de solapamiento por construcción). Documentado en el docblock del controlador.
- **Criterio de prorrateo de cambio de afiliación a mitad de periodo sigue `PENDING DECISION`** — se implementó el mismo criterio de días calendario ya usado para el prorrateo salarial de contrato partido (Fase 9), por simplicidad de ingeniería, no porque exista validación de que la seguridad social colombiana prorratea igual. Ver Grupo 2b del checklist de `PENDING DECISION` arriba.

## Decisiones de implementación no obvias (Fase 9 — Payroll)

- **Alcance recortado a 3 conceptos monetarios** (salario base prorrateado, horas extra autorizadas, deducciones fijas) — Seguridad Social (Fase 10) y el recargo festivo/dominical (Time Calculation nunca produjo esa magnitud) quedan fuera, documentados explícitamente en `10-PAYROLL.md`, no en silencio. Los criterios de aceptación de esta fase (inmutabilidad de cierre, ajuste sin sobrescribir, aislamiento de contrato ambiguo, sin tasas hardcodeadas, solo backend) quedan cubiertos igual con este recorte.
- **`payroll_entry_lines` necesitó `contract_id`** y **`payroll_deduction_plans` necesitó `installment_amount`** y **`payroll_adjustments` necesitó `mechanism`** — columnas que la lista abreviada de "columnas clave" de `05-DATABASE.md` Bloque 7 no incluye pero que la propia regla 2/criterio de aceptación #2 de `10-PAYROLL.md` (contract_id) y la necesidad de no re-derivar con deriva de redondeo (installment_amount) exigen. Se agregaron — llenan un vacío que el mismo documento manda, no es alcance inventado.
- **`payroll_entries`/`payroll_entry_lines` (AJUSTE) NO llevan guarda de inmutabilidad a nivel de ORM** — mismo criterio que `AttendanceAdjustment`: mutables libremente mientras el periodo padre no esté `CLOSED`, disciplina aplicada solo a nivel de servicio. Solo `payroll_adjustments` (INMUTABLE, append-only) recibe la guarda de dos capas duplicada del patrón `AttendanceEvent`/`TimeCalculationRun` (nunca reutilizada, siempre duplicada).
- **`PayrollCalculationService::calculateForEmployee()` gana su propia guarda de estado `closed`**, independiente de `PayrollPeriodService::calculate()` — se descubrió durante el test de integración de la quincena completa que llamar al motor de cálculo directamente (sin pasar por el servicio de ciclo de vida) no tenía ninguna defensa propia contra recalcular un periodo ya cerrado. Corregido en el mismo commit que lo encontró (`cf4ca0e`), ya que amenazaba directamente el criterio de aceptación de inmutabilidad de cierre.
- **`monthly_hours_divisor`/`overtime_surcharge_pct` son claves nuevas y obligatorias en `labor_rule_versions.parameters`** (bloqueante con `MissingLaborRuleParameterException` si faltan) — extensión del contenedor JSON, no invención de un valor legal; Fase 7 ya había anticipado que Payroll agregaría claves de dinero cuando las necesitara de verdad.
- **Fórmula de horas extra**: `tarifa_hora = salario_mensual / monthly_hours_divisor`; `monto = tarifa_hora * (1 + overtime_surcharge_pct) * horas` — el recargo es ADITIVO sobre la tarifa base (125% de la tarifa si el recargo es 25%). Es una interpretación de ingeniería de qué significa convencionalmente "recargo" en sistemas de nómina, no un porcentaje legal colombiano validado.
- **`close()` decrementa `payroll_deduction_plans.remaining` una única vez, nunca en un recálculo** — requirió agregar `deduction_plan_id` a `payroll_entry_lines` (no existía, y sin él no había forma de rastrear qué línea corresponde a qué plan) para que `close()` pudiera identificar exactamente qué planes decrementar.
- **`adjustInNextPeriod()` exige que el periodo destino ya tenga una `PayrollEntry` calculada para el empleado** — si no existe, se trata igual que "no hay periodo siguiente abierto" (`NoOpenNextPayrollPeriodException`). Fabricar una entrada huérfana solo para colgarle la línea de corrección corrompería el significado de sus totales.
- **`reopen()` exige `reason` no vacío a nivel de servicio**, no solo de un futuro Form Request — ADR-026 enmarca la reapertura como el camino excepcional, siempre debe llevar un motivo declarado.

## Decisiones de implementación no obvias (Fase 8 — Overtime/Novedades)

- **`leave_types`/`novelty_types` no tienen CRUD ni UI propios, a propósito** — `25-MVP-SCOPE.md` reserva el "catálogo extendido configurable por empresa" para POST-MVP; esta fase solo crea el esquema (con `company_id` nullable para default de plataforma) y un seeder con los 4 esenciales (AUSENCIA/PERMISO/VACACIONES/INCAPACIDAD, con `code` compartido entre ambas tablas como clave de correlación — no hay FK entre ellas). `holidays` sí tiene CRUD completo porque el roadmap la nombra explícitamente y Payroll (Fase 9) necesita fechas concretas por empresa que un seeder estático no cubre año a año.
- **Calendario colombiano sembrado solo con los 6 festivos fijos, no los 12 móviles/Ley Emiliani** (Reyes Magos, San José, Ascensión, Corpus Christi, Sagrado Corazón, San Pedro y San Pablo, Asunción, Día de la Raza, Todos los Santos, Independencia de Cartagena, Jueves Santo, Viernes Santo) — calcular correctamente el algoritmo de Pascua + el corrimiento al lunes siguiente era alcanzable, pero una fecha festiva legal incorrecta es peor que una faltante; se prefirió no sembrar antes que sembrar mal. Una empresa agrega las que falten manualmente vía el CRUD de `holidays`.
- **`TimeCalculationEngine` (Fase 7) todavía no consulta `holidays`** — ningún criterio de aceptación de esta fase lo exige, y la clasificación de festivo/dominical como magnitud de tiempo sigue bloqueada por el `PENDING DECISION` de legislación ya existente en `09-TIME-CALCULATION.md` (línea 21, sin parámetro de `labor_rule_versions` definido para eso todavía).
- **Justificación de ausencia solo para el día completo, nunca prorrateada** — si el empleado marcó algo real ese día (no es ausencia completa), una `novelty_records` aprobada cubriendo la fecha NO afecta la clasificación; `justified_minutes` queda en 0. Es el único caso documentado explícitamente en "Casos especiales" de `09-TIME-CALCULATION.md`, no se inventó una regla de prorrateo para el resto.
- **`overtime_records` se crea/actualiza de forma idempotente por `(employee_id, shift_id)` dentro de la misma transacción que ya escribe `AttendanceRecord`+`TimeCalculationRun`** — un recálculo nunca toca una fila que ya avanzó más allá de `DETECTED` (una decisión humana de `REQUESTED`/`AUTHORIZED`/`REJECTED`/`PAID` nunca se revierte silenciosamente por volver a correr el motor).
- **`overtime.mark_paid` es un permiso "placeholder" documentado como tal** — la transición a `PAID` según el doc depende de "incluida en una liquidación de `payroll_entries`", que no existe hasta la Fase 9; hasta entonces es una transición manual con su propio permiso, y Fase 9 probablemente necesite disparar `markPaid()` automáticamente durante el cierre de nómina en vez de dejarlo manual.
- **`authorize()` de horas extra no valida `authorized_minutes` contra `requested_minutes`** (¿puede autorizarse más o menos de lo solicitado?) — sin regla documentada en ningún `.ai/`, se dejó deliberadamente sin restricción en vez de inventar un tope, per regla #16 de `AGENTS.md`.
- **`NoveltyRecord.source_type`/`source_id` son atributos planos, no un `morphTo()` de Eloquent** — no hay precedente de `Relation::morphMap()` en este codebase, y forzar uno nuevo solo para esta tabla sería una abstracción no justificada (apunta a tres tablas sin interfaz común: `leave_records`/`overtime_records`/`attendance_adjustments`).

## Decisiones de implementación no obvias (Fase 7 — Time Calculation Engine)

- **Ventana de eventos reales para una fecha**: acotada al/los día(s) calendario que cubre `shifts.date`+`crosses_midnight`, no a un margen de tolerancia inventado — se deriva directamente de que el propio doc atribuye todo el turno a `shifts.date`.
- **Emparejamiento de descansos reales**: solo se resta un par `break_start`→`break_end` completo dentro de la ventana `clock_in`→`clock_out`. Un `break_start` sin `break_end` posterior **no se resta** (cuenta como trabajado) y **no bloquea** el cálculo — nuevo `PENDING DECISION` agregado a `09-TIME-CALCULATION.md` (regla #6), implementado alrededor tal como pide la regla #16 de `AGENTS.md`.
- **Precedencia entre ajustes aprobados sobre el mismo evento**: si dos ajustes `approved` (`modify`/`invalidate`) apuntan al mismo `original_event_id` (una corrección de una corrección), **gana el más reciente por `created_at`**, nunca una fusión campo a campo — nuevo `PENDING DECISION` agregado a `07-ATTENDANCE.md` (Flujo 2, punto 4).
- **`labor_rule_versions.parameters` solo tiene `tolerance_minutes`/`rounding_minutes` por ahora** — nada de ventana nocturna todavía (YAGNI: ningún criterio de aceptación de esta fase la ejercita, ningún consumidor la necesita hasta Payroll/Fase 9-10). Faltan ambas claves → `MissingLaborRuleParameterException`, bloqueante explícito, nunca un default asumido.
- **`TimeCalculationRun` duplica el patrón de inmutabilidad de `AttendanceEvent`, no lo reutiliza** — el `ImmutableBuilder` de `AttendanceEvent` tiene su propia excepción hardcodeada en el cuerpo de sus métodos, así que no es genérico. Se creó `TimeCalculationRunImmutableBuilder`/`TimeCalculationRunImmutableException` como clases hermanas, menor blast radius sobre código Fase 6 ya en producción. Cualquier tabla futura `INMUTABLE` debe replicar el patrón de nuevo (guardas `booted()` + builder propio), no intentar generalizar las clases existentes.
- **Recálculo disparado por un ajuste aprobado nunca aborta la transacción del ajuste**: `AttendanceAdjustmentService` llama a `TimeCalculationEngine::calculateForDate()` **después** de que la transacción propia del ajuste ya confirmó (no anidada), y atrapa cualquiera de las 4 excepciones bloqueantes del motor con un `Log::warning()` — aprobar una corrección de asistencia debe poder completarse aunque la empresa todavía no tenga configurada ninguna `labor_rule_version`. Es el equivalente más cercano a la "señal de ajuste de nómina pendiente" del doc, pero esa señal formal solo existe cuando Payroll (Fase 9) exista; por ahora es logueado-y-omitido, no ignorado en silencio.
- **Sin integración de `novelty_records`**: esa tabla es Fase 8 (que depende de esta Fase 7, no al revés). El caso "ausencia justificada sin marcación" del doc queda fuera de alcance hasta que exista — un día con turno y cero eventos hoy se calcula siempre como ausencia completa (`missing_minutes = planeado`), nunca como justificada.
- **Disparo automático de recálculo al publicar una `labor_rule_version` nueva sigue sin resolver** (`PENDING DECISION` ya existente en `09-TIME-CALCULATION.md`, línea ~55, no tocado). Se implementó alrededor: recálculo on-demand (`calculateForRange`) + disparado por ajuste aprobado (sí está en criterios de aceptación) — nunca un disparo masivo automático al publicar una regla.

## Decisiones de implementación no obvias (Fase 6 — Attendance)

- **Inmutabilidad de `attendance_events`** (regla no negociable, ADR-003: nunca se edita ni se borra) se hace cumplir en dos capas del modelo `AttendanceEvent`, no solo documentalmente: `booted()` registra listeners `updating`/`deleting` que lanzan `AttendanceEventImmutableException`, y un `newEloquentBuilder()` propio (`ImmutableBuilder`) sobreescribe `update()`/`delete()` a nivel de query builder para cubrir también updates/deletes masivos que no disparan eventos de modelo individuales. Es el primer modelo del proyecto con esta guarda; cualquier tabla futura marcada `INMUTABLE` en `05-DATABASE.md` (ej. `time_calculation_runs` en Fase 7) debería replicar el mismo patrón.
- **Deduplicación de eventos de asistencia**: ventana de 1 minuto, mismo empleado + mismo `event_type`. El esquema de `attendance_events` no tiene columna `status`, así que un duplicado detectado **no inserta fila nueva** — se devuelve el evento existente encontrado. El check-then-insert tiene una ventana TOCTOU conocida bajo concurrencia (documentada en el docblock de `AttendanceEventRecorder`); no se agregó constraint de Postgres porque no hay una forma natural de expresar una ventana deslizante de 60s como unique index.
- **Casing de valores tipo-enum**: `event_type`/`source`/`type`/`status` de Attendance usan `snake_case` minúscula (`clock_in`, `web`, `pending`) para quedar consistentes con el resto del codebase (`'assigned'`, `'cancelled'`, `'manual'`), aunque la prosa de `07-ATTENDANCE.md` esté en mayúscula (`CLOCK_IN`, `APPROVED` son conceptuales, no literales de esquema).
- **Primer uso real de `Rule::in()` en el proyecto**: hasta la Fase 6, ningún Form Request validaba un conjunto cerrado de valores porque los strings tipo-enum siempre los escribía código de confianza. `source` en `StoreAttendanceEventRequest` es el primer campo que recibe un valor cerrado desde afuera y de verdad necesita rechazar valores fuera de rango (`source` restringido a `web`/`manual` únicamente en esta fase; `mobile`/`qr`/`api`/`device`/`biometric` quedan reservados para fases posteriores).
- **`attendance.record` (crear un evento normal) es un permiso separado de `attendance.adjust` (corregir uno existente)** — mismo set de roles hoy (`SUPER_ADMIN`/`COMPANY_OWNER`/`ADMIN`/`HR_MANAGER`/`SUPERVISOR`), pero conceptualmente distintos; no reusar uno por el otro si en el futuro divergen.
- **Auto-aprobación de ajustes (ADR-032) se deriva en vivo de `$user->hasPermission('attendance.approve_adjustment')`**, nunca de una lista de roles hardcodeada en `AttendanceAdjustmentService` — así nunca se desincroniza de `RoleSeeder` si los grants cambian.
- **Modelo de fichada confirmado con el usuario**: se evaluó biometría de huella para la Fase 6, pero se decidió mantenerla en su lugar del roadmap (Fase 12, POST-MVP, ADR-036) y construir la Fase 6 tal como estaba especificada — panel operado por RR.HH./supervisor, sin autoservicio del empleado.

## Próximo paso

**Las 16 fases del roadmap MVP están completas en código** (Fase 12/Biometrics sigue `[POST-MVP]` por `25-MVP-SCOPE.md`, deliberadamente fuera de alcance). Lo único que queda del propio checklist de Fase 15 no es código, es el checklist operativo en el dashboard de Laravel Cloud/GitHub (deploy hooks, secrets, Environment de producción con reviewers, ensayo de restauración de backup, prueba de rollback) — ver la fila de Fase 15 arriba y la sección "Decisiones de implementación no obvias (Fase 15)". Una vez completado ese checklist, el sistema queda listo para operar con datos reales de empresas clientes.
